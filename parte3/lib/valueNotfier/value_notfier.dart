import 'package:flutter/material.dart';

class ImcValueNotifer extends StatelessWidget {
  const ImcValueNotifer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Value Notfier"),),
    );
  }
}